import SettingsLayout from "../../layouts/documentation"
import Layout from "../../layouts/main"

# HTML
Built off Sass this can be used with any framework but, if your framework is supported, it's advised to use the offical component


export default ({ children }) => <Layout><SettingsLayout>{children}</SettingsLayout></Layout>

